//Expressão de funções

const soma = function (num1, num2) {
  return num1 + num2;
};
console.log(soma(1, 1));

//arrow functions

function apresentar(nome) {
  return `meu nome é ${Renan}`;
}

const apresentarArrow = (nome) => `meu nome é ${nome}`;
